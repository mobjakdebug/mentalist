# 📖 راهنمای جامع و گام‌به‌گام راه‌اندازی Cloudflare D1 و ورکر بازی «سیاه‌بازی»

این راهنما با جزئیات کامل و دقیق به شما آموزش می‌دهد که چگونه دیتابیس بدون سرور **Cloudflare D1 (Serverless SQLite)** و ورکر ارتباطی بازی را روی حساب کلودفلر خود مستقر کرده و اپلیکیشن اندروید را مستقیماً به آن متصل کنید.

---

## 📑 فهرست مطالب
1. [پیش‌نیازها](#۱-پیشنیازها)
2. [گام ۱: ورود به حساب کلودفلر از طریق ترمینال](#گام-۱-ورود-به-حساب-کلودفلر-از-طریق-ترمینال)
3. [گام ۲: ساخت دیتابیس D1](#گام-۲-ساخت-دیتابیس-d1)
4. [گام ۳: تنظیم فایل wrangler.toml](#گام-۳-تنظیم-فایل-wranglertoml)
5. [گام ۴: اعمال ساختار جداول (Schema) روی دیتابیس ابری](#گام-۴-اعمال-ساختار-جداول-schema-روی-دیتابیس-ابری)
6. [گام ۵: انتشار (Deploy) ورکر روی کلودفلر](#گام-۵-انتشار-deploy-ورکر-روی-کلودفلر)
7. [گام ۶: اتصال اپلیکیشن اندروید به ورکر خودتان](#گام-۶-اتصال-اپلیکیشن-اندروید-به-ورکر-خودتان)
8. [بررسی ساختار پایگاه داده و جداول](#بررسی-ساختار-پایگاه-داده-و-جداول)
9. [مستندات API و متدهای احراز هویت و بازی](#مستندات-api-و-متدهای-احراز-هویت-و-بازی)
10. [حل مشکلات رایج (Troubleshooting)](#حل-مشکلات-رایج-troubleshooting)

---

## ۱. پیش‌نیازها

برای راه‌اندازی این بخش به موارد زیر در رایانه خود نیاز دارید:
1. **Node.js و npm:** نسخه ۱۸ به بالا (با اجرای دستور `node -v` در ترمینال چک کنید).
2. **یک حساب رایگان در وب‌سایت [Cloudflare](https://dash.cloudflare.com/sign-up):** سرویس‌های Workers و D1 در پلن رایگان کلودفلر تا ۵ میلیون درخواست در ماه کاملاً **رایگان** هستند.

---

## گام ۱: ورود به حساب کلودفلر از طریق ترمینال

ابتدا در ترمینال وارد پوشه `cloudflare` پروژه شوید:
```bash
cd cloudflare
```

سپس با ابزار رسمی کلودفلر (Wrangler) به حساب خود متصل شوید:
```bash
npx wrangler login
```
*مرورگر باز می‌شود؛ لاگین کرده و دسترسی را تایید نمایید. در ترمینال پیام `Successfully logged in` نمایش داده خواهد شد.*

---

## گام ۲: ساخت دیتابیس D1

دستور زیر را اجرا کنید تا یک پایگاه داده جدید به نام `siahbazi-db` در کلودفلر ایجاد شود:
```bash
npx wrangler d1 create siahbazi-db
```

پس از چند ثانیه خروجی زیر را مشاهده خواهید کرد:
```text
✅ Successfully created DB 'siahbazi-db' in region EEUR
Created your new database. Using binding name 'DB'

[[d1_databases]]
binding = "DB"
database_name = "siahbazi-db"
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
```

> ⚠️ **نکته کلیدی:** شناسه `database_id` (رشته ۳۶ کاراکتری تولید شده) را کپی کنید!

---

## گام ۳: تنظیم فایل `wrangler.toml`

فایل `cloudflare/wrangler.toml` را در ویرایشگر باز کنید و شناسه کپی‌شده را در بخش `database_id` قرار دهید:

```toml
name = "siahbazi-backend"
main = "worker.js"
compatibility_date = "2024-01-01"

[[d1_databases]]
binding = "DB"
database_name = "siahbazi-db"
database_id = "شناسه_۳۶_کاراکتری_که_در_مرحله_قبل_گرفتید"
```

---

## گام ۴: اعمال ساختار جداول (Schema) روی دیتابیس ابری

تمام ساختار جداول بازی (ثبت‌نام کاربران، اتاق‌ها، کارت‌های ناشناس، چت بازجویی و گزارش‌ها) در فایل `schema.sql` آماده شده است. با دستور زیر آن را روی سرور ابری D1 کلودفلر اعمال کنید:

```bash
npx wrangler d1 execute siahbazi-db --file=schema.sql --remote
```

پس از اجرای دستور، ۱۲ سوال استاندارد روانشناسی و جداول دیتابیس در D1 ثبت می‌شوند.

---

## گام ۵: انتشار (Deploy) ورکر روی کلودفلر

حالا با یک دستور ورکر خود را روی اینترنت مستقر کنید:
```bash
npx wrangler deploy
```

در پایان یک لینک شبیه به آدرس زیر دریافت خواهید کرد:
```text
Total Upload: xx.xx KiB / gzip: xx.xx KiB
Uploaded siahbazi-backend (x.xx sec)
Published siahbazi-backend (x.xx sec)
  https://siahbazi-backend.<نام-کاربری-شما>.workers.dev
Current Deployment ID: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

برای آزمایش صحت اتصال، آدرس را به صورت زیر در مرورگر یا با `curl` باز کنید:
```bash
curl https://siahbazi-backend.<نام-کاربری-شما>.workers.dev/health
```
باید پاسخ JSON با وضعیت `status: "connected"` و تعداد سوالات دریافت کنید:
```json
{
  "status": "connected",
  "service": "Siahbazi Mentalist Backend",
  "database": "Cloudflare D1",
  "questionCount": 12,
  "activeRoomsCount": 0
}
```

---

## گام ۶: اتصال اپلیکیشن اندروید به ورکر خودتان

۱. بازی «سیاه‌بازی» را در گوشی یا شبیه‌ساز باز کنید.
۲. در صفحه ورود/ثبت‌نام یا صفحه اصلی، روی **آیکون چرخ‌دنده یا وضعیت سرور D1** ضربه بزنید.
۳. در کادر باز شده، آدرس ورکر خود را وارد کنید (مثال: `https://siahbazi-backend.<your-subdomain>.workers.dev`).
۴. دکمه **«تست اتصال و ذخیره»** را فشار دهید.
۵. نشانگر بلافاصله سبز شده و تاخیر سرور (Ping) به همراه نام دیتابیس نمایش داده می‌شود.

---

## بررسی ساختار پایگاه داده و جداول

دیتابیس ابری `siahbazi-db` شامل ۸ جدول مهندسی‌شده زیر است:

| نام جدول | توضیحات |
| :--- | :--- |
| `users` | اطلاعات حساب‌های کاربری واقعی (نام کاربری، هش کلمه عبور با SHA-256، آواتار انتخابی، سطح واقعی، سکه و آمار پیروزی) |
| `rooms` | اتاق‌های ساخته شده با کد اختصاصی ۵ رقمی، وضعیت فاز جاری و شناسه بازجو (منتالیست) |
| `players` | بازیکنان متصل به هر لابی همراه با وضعیت آماده‌باش، پینگ زنده و امتیاز راند |
| `questions` | بانک سوالات روانشناسی، چالش‌برانگیز و مچ‌گیری |
| `answers` | پاسخ‌های متنی محرمانه بازیکنان که در فاز بازجویی به صورت کارت ناشناس نمایش داده می‌شود |
| `messages` | پیام‌های چت بازجویی و پرسش‌های پروبینگ به همراه زمان ارسال |
| `reports` | گزارش‌های تخلف، الفاظ نامناسب یا ترولینگ ثبت‌شده توسط بازیکنان |

---

## مستندات API و متدهای احراز هویت و بازی

### ۱. ثبت‌نام حساب جدید (Register)
* **آدرس:** `POST /api/auth/register`
* **ورودی (Body):**
```json
{
  "username": "کارآگاه_سایه",
  "email": "detective@example.com",
  "password": "your_secure_password",
  "avatarEmoji": "🕵️"
}
```
* **خروجی:** اطلاعات پروفایل کاربری با مقادیر اولیه (Level 1, 0 XP, 0 Coins) و توکن جلسه.

### ۲. ورود به حساب (Login)
* **آدرس:** `POST /api/auth/login`
* **ورودی (Body):**
```json
{
  "username": "کارآگاه_سایه",
  "password": "your_secure_password"
}
```

### ۳. ایجاد اتاق جدید (Create Room)
* **آدرس:** `POST /api/rooms/create`
* **ورودی:** `{ "playerName": "...", "avatarEmoji": "🎭", "level": 1 }`
* **خروجی:** کد اختصاصی ۵ کاراکتری (مانند `K8M2P`) و شناسه بازیکن میزبان.

### ۴. پیوستن به اتاق (Join Room)
* **آدرس:** `POST /api/rooms/join`
* **ورودی:** `{ "roomCode": "K8M2P", "playerName": "...", "avatarEmoji": "👁️", "level": 1 }`

### ۵. ارسال پاسخ کارت محرمانه (Submit Answer)
* **آدرس:** `POST /api/rooms/{code}/answers`
* **ورودی:** `{ "playerId": "...", "answerText": "پاسخ روانشناسی بازیکن" }`

### ۶. چت زنده بازجویی (Send Chat)
* **آدرس:** `POST /api/rooms/{code}/chat`
* **ورودی:** `{ "playerId": "...", "playerName": "...", "text": "...", "isMentalist": true }`

### ۷. ثبت حدس‌های منتالیست (Submit Guess)
* **آدرس:** `POST /api/rooms/{code}/guess`
* **ورودی:** تطابق کارت‌ها با بازیکنان مظنون.

---

## حل مشکلات رایج (Troubleshooting)

### خطای `D1_NOT_BOUND` یا خطای ۵۰۰
**علت:** بایندینگ `[[d1_databases]]` در فایل `wrangler.toml` تعریف نشده یا شناسه `database_id` اشتباه است.
**راه‌حل:** مقدار `database_id` را با دستور `npx wrangler d1 list` بررسی و مجدداً `npx wrangler deploy` را اجرا کنید.

### عدم اتصال برنامه یا خطای شبکه
**علت:** ممکن است به دلیل محدودیت‌های اینترنتی دسترسی به دامنه `workers.dev` مسدود شده باشد.
**راه‌حل:** می‌توانید یک دامنه شخصی (Custom Domain) در بخش **Workers & Pages > Settings > Triggers** اضافه کنید یا از DNS کلودفلر بهره ببرید.
